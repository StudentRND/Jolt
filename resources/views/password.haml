- extends "template.haml"
- block section "password"
- block content
  .login
    %h2 Change Your Password
    %form(method="post")
      .field
        %label(for="lookup") New Password
        %input(type="password" name="password" placeholder="hunter2" required)

      %input(type="submit" value="Change Password")
      #{csrf_field()}
