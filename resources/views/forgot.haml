- extends "template.haml"
- block section "login"
- block content
  .login
    %h2 Forgot Password
    %form(method="post")
      .field
        %label(for="lookup") Username or Email
        %input(type="text" name="lookup" placeholder="kindhearted-squid" required)

      %input(type="submit" value="Login")
      #{csrf_field()}
